Example
====

## Install

```bash
$ cd app/ios

$ bundle install
$ bundle exec pod install
```

Macbook M1 Install

```bash
$ cd app/ios

$ arch -arm64 bundle install
$ arch -arm64 bundle exec pod install
```